# kushop

Frontend ของ KU Shop ใช้ React, React Router และ Vite โดยยังคงโครงสร้าง `src/components`, `src/router` และ `src/stores` ไว้ ส่วน backend ใช้ Express แยกอยู่ในโฟลเดอร์ `backend`

## เริ่มใช้งาน

```sh
npm install
npm run dev
```

## สร้าง production build

```sh
npm run build
```
